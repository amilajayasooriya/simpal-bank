spring:
  datasource:
    url: jdbc:h2:mem:simple-bank-test-db;DB_CLOSE_DELAY=-1
    username: simplebank-user
    password: simplebank-password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect